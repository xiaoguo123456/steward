# AI 事管家移动端设计准则

本文是 `apps/mobile` 的视觉与交互执行基线。产品行为仍以 `docs/功能规格说明.md` 和 `docs/产品设计说明.md` 为准；本文件负责把跨页面的一致性要求落实为可复用规则。

## 1. 产品气质

AI 事管家是安静、可靠、带轻微陪伴感的生活工具，不是企业 Dashboard，也不是用装饰填满页面的通用 AI 模板。

- 内容优先：照片、任务、日程、路线、票据、食谱和真实统计承担丰富度。
- 操作克制：每个页面只突出一个主要动作，次要能力渐进披露。
- 信任优先：无数据时诚实留白；禁止用示例统计冒充用户事实。
- 原生优先：高频核心流程保持 React Native；平台材质仅用于导航等边界清晰的位置。

## 2. 色彩方向

采用“白色画布 + 冷中性表面 + 品牌绿操作”的全局体系，不将整个 App 改成淡蓝色。

- `background` 用于页面主画布，`surfaceSubtle` 用于输入、状态和轻量分组。
- `primary` / `primaryStrong` 只承载主要操作、选中状态与导航识别。
- 淡蓝保留给信息、灵感、心情等局部语义，不作为所有卡片的默认底色。
- 品类色仅用于小面积图标、标签或数据标记，不铺满大容器。
- 文本与常用表面的对比度至少满足 WCAG AA 小字号标准；禁止用低对比灰色制造“高级感”。

## 3. 字体与信息层级

- 字体只使用 `fontFamily` Token：iOS 为苹方，Android 为 Noto Sans CJK SC，Web 使用系统兼容栈。
- 页面标题、详情标题、分区标题、正文、标签、元信息和指标必须复用 `typography`。
- 一个页面只保留一个页面级标题。分区标题使用 `SectionTitle`，不要在功能内重复造同名组件。
- 数字指标使用等宽数字，但只有真实、可解释的数据才允许成为视觉焦点。
- 常规页面只保留完成判断、操作或异常恢复所必需的文字。实现边界、未来规划、研发状态、重复规则说明和自我解释不进入界面；不可逆后果放在真正执行危险操作时的确认流程中。
- 字段标签、错误原因、权限用途和必要空状态不属于冗余说明，仍需清晰可读；占位符不能替代关键字段标签。

## 4. 布局与容器

- 页面水平留白默认 16；间距、圆角只使用 `spacing` 与 `radius`。
- 列表优先采用共面分隔线，不把每一行都做成悬浮卡片。
- 卡片只用于需要独立聚合、可整体点击或需要明显状态边界的内容。
- 阴影仅用于浮层、拖拽对象和确有层级差的元素；普通内容卡片不使用宽阴影。
- 可触控目标至少 44 × 44 dp，并为动态字号和长文本保留伸展空间。

## 5. 组件语言

当前统一基座是 Expo UI、Hugeicons 和 `apps/mobile/src/components/ui` 内部原语。现阶段不新增另一套成品 UI Kit。

- 按钮使用 `AppButton`。
- 页面与导航头使用 `AppScreen`、`PageHeader`、`NavHeader`。
- 分区标题使用 `SectionTitle`，筛选使用 `FilterChip`。
- 空、错误和无结果状态使用 `StatePanel`。
- 底部编辑与选择使用 `ModalSheet`。
- 图标只经 `AppIcon` 语义适配层使用 Hugeicons，不混用第二套图标。

当两类以上客户端确实需要共享稳定原语时，再把成熟组件抽取到 `packages/ui`；不要先引入框架再寻找使用场景。

## 6. 状态设计

每个数据页面必须区分以下状态，文案和动作不能混用：

- 首次为空：解释功能价值，并提供创建第一项的明确动作。
- 筛选为空：保留用户数据语境，并提供清除筛选。
- 已完成为空：说明当前工作已经完成，但不能把从未创建说成“已完成”。
- 加载中：骨架应尽量贴近最终布局；只有瞬时、局部动作可只用转圈。
- 加载失败：说明发生了什么，保留已有内容，并提供重试。
- 离线或版本冲突：使用明确的恢复路径，禁止用通用“出错了”覆盖原因。

## 7. 图片、插画与 AI 角色

- 增加图片时优先选择有信息价值的内容资产：回忆照片、食谱缩略图、运动路线、票据缩略图和真实图表。
- 不引入通用空状态插画或 clip-art 库；它们会稀释个人内容并造成模板感。
- 图片必须定义授权来源、裁切比例、占位、失败、缓存和隐私策略。
- AI 角色只在确有 AI 来源、待确认问题或总结身份时出现；不能长期遮挡主按钮、媒体或状态面板。

## 8. 禁止模式

- 禁止把全局主题整体染成淡蓝或给每个模块单独换一套主题。
- 禁止在没有真实数据时展示虚构趋势、洞察或完成状态。
- 禁止用更多圆角卡片、渐变、阴影和装饰图标替代信息架构调整。
- 禁止 Feature 直接使用第三方组件默认皮肤或绕过公共原语。
- 禁止为视觉统一牺牲可读性、触控尺寸、原生权限边界和确定性手工路径。
- 禁止把“本地演示、即将开放、发布后不可编辑、数据如何处理”等开发注释或规则复述长期摆在正常内容页；仅在用户必须知情并作出选择的时刻展示最小必要信息。

## 9. 渐进治理顺序

1. 先修正错误语义、虚构数据、遮挡与可访问性问题。
2. 再统一 Token、公共状态、标题、列表与表单原语。
3. 然后减少首页等高负荷页面的等权入口，强化主要任务。
4. 最后按内容价值补充真实图片、缩略图和数据可视化。

所有新增功能都应在评审时说明使用了哪些公共原语、六类状态如何覆盖，以及 AI 能力如何复用正式领域契约和确认流程。
