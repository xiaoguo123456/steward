{
  "name": "@steward/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI、错误码、Fixture 与跨语言测试向量的唯一来源",
  "scripts": {
    "lint": "redocly lint openapi/openapi.yaml",
    "bundle": "redocly bundle openapi/openapi.yaml -o dist/openapi.bundle.yaml",
    "check": "pnpm run lint && pnpm run bundle"
  },
  "devDependencies": {
    "@redocly/cli": "^2.5.0"
  }
}
