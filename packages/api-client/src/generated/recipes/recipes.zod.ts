/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * 按关键词、分类与用餐时段筛选。过敏原过滤是确定性硬条件，
 * 排序不得覆盖它：exclude_allergens 命中的菜谱一律不返回。
 * @summary 查询菜谱
 */

export const listRecipesQueryLimitDefault = 20;
export const listRecipesQueryLimitMax = 100;



export const ListRecipesQueryParams = zod.object({
  "q": zod.string().optional().describe('按菜名或食材搜索。'),
  "category": zod.enum(['recommended', 'quick', 'seasonal', 'fat_loss', 'muscle_gain', 'steady_sugar']).optional(),
  "meal_slot": zod.enum(['breakfast', 'lunch', 'dinner']).optional(),
  "exclude_allergens": zod.array(zod.string()).optional().describe('排除含有这些过敏原的菜谱。这是硬过滤，不是排序权重。\n'),
  "max_minutes": zod.number().int().min(1).optional().describe('只要这个时间内能做完的。'),
  "limit": zod.number().int().min(1).max(listRecipesQueryLimitMax).default(listRecipesQueryLimitDefault).describe('单页条数。')
})







export const ListRecipesResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "title": zod.string(),
  "summary": zod.string().nullish(),
  "image_url": zod.string().nullish().describe('没有明确图片权利时为 null，客户端展示占位而不是随便找一张图。\n'),
  "servings": zod.number().int().min(1),
  "duration_minutes": zod.number().int().min(1),
  "difficulty": zod.enum(['easy', 'medium', 'hard']),
  "nutrition": zod.object({
  "calories": zod.number(),
  "protein_g": zod.number(),
  "carbs_g": zod.number(),
  "fiber_g": zod.number()
}).describe('每份的营养估算。它是估算值，不是营养档案，也不构成任何健康承诺。\n'),
  "meal_slots": zod.array(zod.enum(['breakfast', 'lunch', 'dinner'])),
  "categories": zod.array(zod.enum(['recommended', 'quick', 'seasonal', 'fat_loss', 'muscle_gain', 'steady_sugar'])),
  "goals": zod.array(zod.enum(['balanced', 'fat_loss', 'muscle_gain', 'steady_sugar']).describe('steady_sugar 只用于推荐少添加糖、优先全谷物、增加膳食纤维的日常菜谱，\n不提供疾病诊断、治疗承诺或用药建议。\n')),
  "tags": zod.array(zod.string()),
  "allergens": zod.array(zod.string()).describe('过敏原是确定性硬过滤条件，任何排序或推荐都不得覆盖它。\n'),
  "ingredients": zod.array(zod.object({
  "name": zod.string(),
  "amount": zod.string().describe('自由文本，例如「300 克」「适量」。生成购物清单时按名称合并。'),
  "group": zod.enum(['produce', 'protein', 'staple', 'seasoning']).describe('与购物清单的品类分开：这里是做菜时的分组，不是超市货架。')
})).min(1),
  "steps": zod.array(zod.object({
  "title": zod.string(),
  "description": zod.string(),
  "timer_minutes": zod.number().int().nullish().describe('这一步需要计时时给出分钟数，烹饪模式据此提供倒计时。')
})).min(1),
  "source": zod.object({
  "name": zod.string().describe('内容提供方。'),
  "author": zod.string().nullish(),
  "url": zod.string().nullish().describe('原始出处。'),
  "license": zod.string().describe('授权范围，例如 CC-BY-4.0、平台自有内容。'),
  "license_url": zod.string().nullish(),
  "image_credit": zod.string().nullish().describe('图片的权利说明。没有明确权利的图片不下发 image_url。'),
  "content_version": zod.string().describe('内容版本。来源更新时递增，客户端据此判断缓存是否过期。')
}).describe('内容来源与授权。字段不全的菜谱不允许进库：\n不知道来源和授权范围，就等于不知道自己有没有权利展示它。\n')
})),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取菜谱详情
 */
export const GetRecipeParams = zod.object({
  "recipe_id": zod.string()
})







export const GetRecipeResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "title": zod.string(),
  "summary": zod.string().nullish(),
  "image_url": zod.string().nullish().describe('没有明确图片权利时为 null，客户端展示占位而不是随便找一张图。\n'),
  "servings": zod.number().int().min(1),
  "duration_minutes": zod.number().int().min(1),
  "difficulty": zod.enum(['easy', 'medium', 'hard']),
  "nutrition": zod.object({
  "calories": zod.number(),
  "protein_g": zod.number(),
  "carbs_g": zod.number(),
  "fiber_g": zod.number()
}).describe('每份的营养估算。它是估算值，不是营养档案，也不构成任何健康承诺。\n'),
  "meal_slots": zod.array(zod.enum(['breakfast', 'lunch', 'dinner'])),
  "categories": zod.array(zod.enum(['recommended', 'quick', 'seasonal', 'fat_loss', 'muscle_gain', 'steady_sugar'])),
  "goals": zod.array(zod.enum(['balanced', 'fat_loss', 'muscle_gain', 'steady_sugar']).describe('steady_sugar 只用于推荐少添加糖、优先全谷物、增加膳食纤维的日常菜谱，\n不提供疾病诊断、治疗承诺或用药建议。\n')),
  "tags": zod.array(zod.string()),
  "allergens": zod.array(zod.string()).describe('过敏原是确定性硬过滤条件，任何排序或推荐都不得覆盖它。\n'),
  "ingredients": zod.array(zod.object({
  "name": zod.string(),
  "amount": zod.string().describe('自由文本，例如「300 克」「适量」。生成购物清单时按名称合并。'),
  "group": zod.enum(['produce', 'protein', 'staple', 'seasoning']).describe('与购物清单的品类分开：这里是做菜时的分组，不是超市货架。')
})).min(1),
  "steps": zod.array(zod.object({
  "title": zod.string(),
  "description": zod.string(),
  "timer_minutes": zod.number().int().nullish().describe('这一步需要计时时给出分钟数，烹饪模式据此提供倒计时。')
})).min(1),
  "source": zod.object({
  "name": zod.string().describe('内容提供方。'),
  "author": zod.string().nullish(),
  "url": zod.string().nullish().describe('原始出处。'),
  "license": zod.string().describe('授权范围，例如 CC-BY-4.0、平台自有内容。'),
  "license_url": zod.string().nullish(),
  "image_credit": zod.string().nullish().describe('图片的权利说明。没有明确权利的图片不下发 image_url。'),
  "content_version": zod.string().describe('内容版本。来源更新时递增，客户端据此判断缓存是否过期。')
}).describe('内容来源与授权。字段不全的菜谱不允许进库：\n不知道来源和授权范围，就等于不知道自己有没有权利展示它。\n')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

