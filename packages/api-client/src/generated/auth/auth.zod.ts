/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * @summary 发送登录验证码
 */
export const requestPhoneCodeBodyPhoneRegExp = new RegExp('^1[3-9][0-9]{9}$');
export const requestPhoneCodeBodyPurposeDefault = `login`;

export const RequestPhoneCodeBody = zod.object({
  "phone": zod.string().regex(requestPhoneCodeBodyPhoneRegExp).describe('中国大陆手机号，11 位数字。'),
  "purpose": zod.enum(['login', 'change_phone']).default(requestPhoneCodeBodyPurposeDefault).describe('验证码用途，用于区分登录与换绑等场景的限流口径。')
})

export const requestPhoneCodeResponseDataDevCodeRegExp = new RegExp('^[0-9]{6}$');


export const RequestPhoneCodeResponse = zod.object({
  "data": zod.object({
  "expires_in_seconds": zod.number().int().describe('验证码有效期。'),
  "resend_after_seconds": zod.number().int().describe('允许再次发送前需要等待的秒数。'),
  "dev_code": zod.string().regex(requestPhoneCodeResponseDataDevCodeRegExp).nullish().describe('仅在本地或测试环境返回的固定验证码，生产环境恒为 null。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 手机号验证码登录
 */
export const loginBodyPhoneRegExp = new RegExp('^1[3-9][0-9]{9}$');
export const loginBodyCodeRegExp = new RegExp('^[0-9]{6}$');


export const LoginBody = zod.object({
  "phone": zod.string().regex(loginBodyPhoneRegExp),
  "code": zod.string().regex(loginBodyCodeRegExp),
  "timezone": zod.string().optional().describe('客户端当前 IANA 时区，用于首次初始化默认值。')
})

export const LoginResponse = zod.object({
  "data": zod.object({
  "tokens": zod.object({
  "access_token": zod.string(),
  "refresh_token": zod.string(),
  "access_expires_at": zod.string().datetime({"offset":true}),
  "refresh_expires_at": zod.string().datetime({"offset":true})
}),
  "user": zod.object({
  "id": zod.string(),
  "phone": zod.string().describe('已脱敏的手机号，中间四位为星号。'),
  "display_name": zod.string(),
  "avatar_url": zod.string().nullish(),
  "timezone": zod.string().describe('用户时区，所有日期语义换算的基准。'),
  "initialized": zod.boolean().describe('是否已完成首次初始化流程。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "is_new_user": zod.boolean().describe('为 true 时 App 进入初始化流程。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 用 Refresh Token 换取新的 Access Token
 */
export const RefreshTokenBody = zod.object({
  "refresh_token": zod.string()
})

export const RefreshTokenResponse = zod.object({
  "data": zod.object({
  "access_token": zod.string(),
  "refresh_token": zod.string(),
  "access_expires_at": zod.string().datetime({"offset":true}),
  "refresh_expires_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 注销当前 Refresh Token
 */
export const LogoutResponse = zod.object({
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

