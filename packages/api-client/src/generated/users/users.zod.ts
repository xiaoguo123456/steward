/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * 受理事务会消费重新认证凭证、把账号置为 deletion_pending、撤销全部会话、
 * 取消未完成异步操作并登记 retention Worker。相同幂等键与相同请求可安全重放，
 * 即使账号已进入删除状态也只会返回首次受理结果。
 * @summary 正式受理账号与数据删除
 */
export const requestAccountDeletionHeaderIdempotencyKeyMin = 8;
export const requestAccountDeletionHeaderIdempotencyKeyMax = 128;



export const RequestAccountDeletionHeader = zod.object({
  "Idempotency-Key": zod.string().min(requestAccountDeletionHeaderIdempotencyKeyMin).max(requestAccountDeletionHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const RequestAccountDeletionBody = zod.object({
  "reauth_token": zod.string(),
  "acknowledgement": zod.boolean().describe('用户已阅读删除范围、不可恢复影响与备份到期说明。')
})

export const RequestAccountDeletionResponse = zod.object({
  "data": zod.object({
  "deletion_request_id": zod.string(),
  "status_token": zod.string().describe('仅用于删除状态查询；客户端必须存入安全存储。'),
  "status": zod.enum(['accepted', 'revoking_sessions', 'purging_assets', 'purging_derivatives', 'purging_primary', 'completed', 'failed']),
  "accepted_at": zod.string().datetime({"offset":true}),
  "backup_expires_at": zod.string().datetime({"offset":true}).describe('备份系统预计完成不可恢复清除的最晚时间。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 使用独立凭证查询删除状态
 */
export const GetAccountDeletionStatusParams = zod.object({
  "deletion_request_id": zod.string()
})

export const getAccountDeletionStatusHeaderDeletionStatusTokenMin = 32;
export const getAccountDeletionStatusHeaderDeletionStatusTokenMax = 256;



export const GetAccountDeletionStatusHeader = zod.object({
  "Deletion-Status-Token": zod.string().min(getAccountDeletionStatusHeaderDeletionStatusTokenMin).max(getAccountDeletionStatusHeaderDeletionStatusTokenMax).describe('删除受理时一次性下发的独立状态凭证，不是登录 Token。')
})

export const GetAccountDeletionStatusResponse = zod.object({
  "data": zod.object({
  "deletion_request_id": zod.string(),
  "status": zod.enum(['accepted', 'revoking_sessions', 'purging_assets', 'purging_derivatives', 'purging_primary', 'completed', 'failed']),
  "accepted_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}),
  "completed_at": zod.string().datetime({"offset":true}).nullish(),
  "backup_expires_at": zod.string().datetime({"offset":true}),
  "public_error": zod.string().nullish().describe('仅包含可公开给用户的失败说明，不含内部资源或身份信息。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取当前用户资料
 */
export const GetCurrentUserResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "phone": zod.string().describe('已脱敏的手机号，中间四位为星号。'),
  "display_name": zod.string(),
  "avatar_url": zod.string().nullish(),
  "timezone": zod.string().describe('用户时区，所有日期语义换算的基准。'),
  "initialized": zod.boolean().describe('是否已完成首次初始化流程。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 修改当前用户资料
 */
export const updateCurrentUserBodyDisplayNameMax = 40;



export const UpdateCurrentUserBody = zod.object({
  "display_name": zod.string().min(1).max(updateCurrentUserBodyDisplayNameMax).optional(),
  "avatar_url": zod.string().nullish(),
  "timezone": zod.string().optional()
})

export const UpdateCurrentUserResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "phone": zod.string().describe('已脱敏的手机号，中间四位为星号。'),
  "display_name": zod.string(),
  "avatar_url": zod.string().nullish(),
  "timezone": zod.string().describe('用户时区，所有日期语义换算的基准。'),
  "initialized": zod.boolean().describe('是否已完成首次初始化流程。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取显式偏好设置
 */
export const GetUserPreferencesResponse = zod.object({
  "data": zod.object({
  "week_start": zod.enum(['monday', 'sunday']).describe('周视图起始日。'),
  "work_day_start": zod.string().describe('工作时间开始，格式 HH:MM，用于安排建议与日历高亮。'),
  "work_day_end": zod.string(),
  "default_reminder_local_time": zod.string().describe('全天 Event 与 due_date 提醒的默认当地触发时间。'),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 修改显式偏好设置
 */
export const UpdateUserPreferencesBody = zod.object({
  "week_start": zod.enum(['monday', 'sunday']).optional(),
  "work_day_start": zod.string().optional(),
  "work_day_end": zod.string().optional(),
  "default_reminder_local_time": zod.string().optional()
})

export const UpdateUserPreferencesResponse = zod.object({
  "data": zod.object({
  "week_start": zod.enum(['monday', 'sunday']).describe('周视图起始日。'),
  "work_day_start": zod.string().describe('工作时间开始，格式 HH:MM，用于安排建议与日历高亮。'),
  "work_day_end": zod.string(),
  "default_reminder_local_time": zod.string().describe('全天 Event 与 due_date 提醒的默认当地触发时间。'),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取 AI 开关
 */
export const GetAiSettingsResponse = zod.object({
  "data": zod.object({
  "capture_parse_enabled": zod.boolean().describe('关闭后 Capture 只保存原始输入，不调用 AI 解析。'),
  "suggestion_enabled": zod.boolean().describe('关闭后不生成任何 AI 建议。'),
  "memory_learning_enabled": zod.boolean().describe('关闭后不再生成新的长期记忆建议。'),
  "mood_journal_ai_enabled": zod.boolean().describe('用户是否单独允许把本次主动提交或选择的最小心情日记正文发送给 AI Provider，\n用于排版润色、保存后追问或阶段回望；默认关闭，可随时撤回。\n'),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 修改 AI 开关
 */
export const UpdateAiSettingsBody = zod.object({
  "capture_parse_enabled": zod.boolean().optional(),
  "suggestion_enabled": zod.boolean().optional(),
  "memory_learning_enabled": zod.boolean().optional(),
  "mood_journal_ai_enabled": zod.boolean().optional()
})

export const UpdateAiSettingsResponse = zod.object({
  "data": zod.object({
  "capture_parse_enabled": zod.boolean().describe('关闭后 Capture 只保存原始输入，不调用 AI 解析。'),
  "suggestion_enabled": zod.boolean().describe('关闭后不生成任何 AI 建议。'),
  "memory_learning_enabled": zod.boolean().describe('关闭后不再生成新的长期记忆建议。'),
  "mood_journal_ai_enabled": zod.boolean().describe('用户是否单独允许把本次主动提交或选择的最小心情日记正文发送给 AI Provider，\n用于排版润色、保存后追问或阶段回望；默认关闭，可随时撤回。\n'),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 需要同时提供当前手机号与新手机号的验证码，两者都用
 * `purpose=change_phone` 获取。
 *
 * 成功后其他设备上的登录会失效，当前设备继续可用。
 * @summary 更换绑定手机号
 */
export const changePhoneHeaderIdempotencyKeyMin = 8;
export const changePhoneHeaderIdempotencyKeyMax = 128;



export const ChangePhoneHeader = zod.object({
  "Idempotency-Key": zod.string().min(changePhoneHeaderIdempotencyKeyMin).max(changePhoneHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const changePhoneBodyCurrentCodeRegExp = new RegExp('^[0-9]{6}$');
export const changePhoneBodyNewPhoneRegExp = new RegExp('^1[3-9][0-9]{9}$');
export const changePhoneBodyNewCodeRegExp = new RegExp('^[0-9]{6}$');


export const ChangePhoneBody = zod.object({
  "current_code": zod.string().regex(changePhoneBodyCurrentCodeRegExp).describe('发到当前绑定手机号的验证码。'),
  "new_phone": zod.string().regex(changePhoneBodyNewPhoneRegExp),
  "new_code": zod.string().regex(changePhoneBodyNewCodeRegExp).describe('发到新手机号的验证码。')
}).describe('更换绑定手机号。\n\n\*\*要同时验证旧号与新号\*\*：这个产品的账号就是手机号（登录＝手机号＋验证码），\n换绑是权限最高的操作。只验新号的话，一个被劫持的会话就能永久接管账号，\n而真正的机主再也登不进来——他的号码已经不再对应任何账号。\n要求旧号的验证码意味着「光有会话不够」。\n\n两个验证码都要用 purpose=change_phone 获取。\n成功后其他设备上的登录会被登出，当前设备不受影响。\n')

export const ChangePhoneResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "phone": zod.string().describe('已脱敏的手机号，中间四位为星号。'),
  "display_name": zod.string(),
  "avatar_url": zod.string().nullish(),
  "timezone": zod.string().describe('用户时区，所有日期语义换算的基准。'),
  "initialized": zod.boolean().describe('是否已完成首次初始化流程。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true})
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 服务端已经知道你是谁，因此不需要也不接受手机号参数——
 * 手机号是脱敏下发的（`138****8000`），客户端本来就拿不到完整号码。
 *
 * 发出的验证码用途是 `change_phone`，登录码不能拿来换绑，反之亦然。
 * @summary 给当前绑定的手机号发送换绑验证码
 */
export const requestCurrentPhoneCodeResponseDataDevCodeRegExp = new RegExp('^[0-9]{6}$');


export const RequestCurrentPhoneCodeResponse = zod.object({
  "data": zod.object({
  "expires_in_seconds": zod.number().int().describe('验证码有效期。'),
  "resend_after_seconds": zod.number().int().describe('允许再次发送前需要等待的秒数。'),
  "dev_code": zod.string().regex(requestCurrentPhoneCodeResponseDataDevCodeRegExp).nullish().describe('仅在本地或测试环境返回的固定验证码，生产环境恒为 null。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

