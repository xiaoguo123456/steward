/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * 只返回真正说过话的对话；没有消息的空对话不出现在历史里。
 * @summary 读取对话列表
 */
export const listThreadsQueryIncludeArchivedDefault = false;
export const listThreadsQueryLimitDefault = 20;
export const listThreadsQueryLimitMax = 100;



export const ListThreadsQueryParams = zod.object({
  "include_archived": zod.boolean().default(listThreadsQueryIncludeArchivedDefault),
  "cursor": zod.string().optional().describe('上一页返回的不透明游标。App 不得解析其内容。'),
  "limit": zod.number().int().min(1).max(listThreadsQueryLimitMax).default(listThreadsQueryLimitDefault).describe('单页条数。')
})

export const ListThreadsResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "title": zod.string().describe('由首条用户消息生成，用户可以修改。'),
  "status": zod.enum(['active', 'archived', 'deleted']),
  "last_message_seq": zod.number().int().optional().describe('Thread 内最新消息序号，客户端据此判断是否有新内容。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
})),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 客户端应当在用户真正发出第一条消息时才调用它：
 * 打开面板就建对话会在历史里堆一串没有内容的空壳。
 * 无标题且未显式要求新建时，按用户时区复用当地自然日内
 * 最近发生用户消息的 active Thread；不是按滚动分钟数判断。
 * @summary 开始一次对话
 */
export const createThreadHeaderIdempotencyKeyMin = 8;
export const createThreadHeaderIdempotencyKeyMax = 128;



export const CreateThreadHeader = zod.object({
  "Idempotency-Key": zod.string().min(createThreadHeaderIdempotencyKeyMin).max(createThreadHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const createThreadBodyForceNewDefault = false;

export const CreateThreadBody = zod.object({
  "title": zod.string().optional(),
  "force_new": zod.boolean().nullish().default(createThreadBodyForceNewDefault).describe('为 true 时一定新建。缺省时按用户时区复用当地自然日内\n最近发生用户消息的 active Thread。\n')
})

export const CreateThreadResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "title": zod.string().describe('由首条用户消息生成，用户可以修改。'),
  "status": zod.enum(['active', 'archived', 'deleted']),
  "last_message_seq": zod.number().int().optional().describe('Thread 内最新消息序号，客户端据此判断是否有新内容。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 按用户资料中的 IANA 时区计算当地自然日，返回当天最近发生用户消息的
 * active Thread。没有时返回 `data: null`，且不会创建空 Thread。
 * Assistant 回复跨过午夜完成不会单独改变 Thread 的自然日归属。
 * @summary 读取今天默认恢复的对话
 */
export const GetCurrentThreadResponse = zod.object({
  "data": zod.union([zod.object({
  "id": zod.string(),
  "title": zod.string().describe('由首条用户消息生成，用户可以修改。'),
  "status": zod.enum(['active', 'archived', 'deleted']),
  "last_message_seq": zod.number().int().optional().describe('Thread 内最新消息序号，客户端据此判断是否有新内容。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),zod.null()]).describe('当天还没有发生用户消息时为 null。'),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取对话摘要
 */
export const GetThreadParams = zod.object({
  "thread_id": zod.string()
})

export const GetThreadResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "title": zod.string().describe('由首条用户消息生成，用户可以修改。'),
  "status": zod.enum(['active', 'archived', 'deleted']),
  "last_message_seq": zod.number().int().optional().describe('Thread 内最新消息序号，客户端据此判断是否有新内容。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 归档只影响默认列表展示，不会让待确认建议自动执行。
 * @summary 修改标题或归档状态
 */
export const UpdateThreadParams = zod.object({
  "thread_id": zod.string()
})

export const UpdateThreadHeader = zod.object({
  "If-Match": zod.string().optional().describe('目标资源的 version。提供后与服务端当前版本不一致时返回 VERSION_CONFLICT。\n并发编辑场景应始终携带。\n')
})

export const updateThreadBodyTitleMax = 60;



export const UpdateThreadBody = zod.object({
  "title": zod.string().min(1).max(updateThreadBodyTitleMax).optional(),
  "status": zod.enum(['active', 'archived', 'deleted']).optional()
})

export const UpdateThreadResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "title": zod.string().describe('由首条用户消息生成，用户可以修改。'),
  "status": zod.enum(['active', 'archived', 'deleted']),
  "last_message_seq": zod.number().int().optional().describe('Thread 内最新消息序号，客户端据此判断是否有新内容。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 按保留策略清理消息与派生摘要。
 * 已经执行的业务实体及其 Activity 不随对话删除。
 * @summary 删除对话内容
 */
export const DeleteThreadParams = zod.object({
  "thread_id": zod.string()
})

export const deleteThreadHeaderIdempotencyKeyMin = 8;
export const deleteThreadHeaderIdempotencyKeyMax = 128;



export const DeleteThreadHeader = zod.object({
  "Idempotency-Key": zod.string().min(deleteThreadHeaderIdempotencyKeyMin).max(deleteThreadHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const DeleteThreadResponse = zod.object({
  "data": zod.object({
  "affected_resources": zod.array(zod.object({
  "type": zod.enum(['task', 'event', 'project', 'note', 'record', 'tracker', 'task_list', 'capture', 'today', 'calendar', 'activity', 'assistant_thread', 'action_proposal', 'memory', 'memory_moment', 'person', 'person_interaction', 'recipe', 'meal_plan', 'diet_profile']).describe('受影响资源类型，App 据此精确失效缓存。'),
  "id": zod.string().nullable().describe('为空表示该类型的集合查询整体失效。')
})),
  "activity_batch_id": zod.string().nullish()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
}).describe('删除等不返回实体的写操作响应，携带需要失效的资源列表。')

/**
 * @summary 读取对话消息
 */
export const ListMessagesParams = zod.object({
  "thread_id": zod.string()
})

export const listMessagesQueryLimitDefault = 20;
export const listMessagesQueryLimitMax = 100;



export const ListMessagesQueryParams = zod.object({
  "cursor": zod.string().optional().describe('上一页返回的不透明游标。App 不得解析其内容。'),
  "limit": zod.number().int().min(1).max(listMessagesQueryLimitMax).default(listMessagesQueryLimitDefault).describe('单页条数。')
})

export const ListMessagesResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "thread_id": zod.string(),
  "message_seq": zod.number().int().describe('Thread 内单调递增序号。消息一经展示不原地改写。'),
  "role": zod.enum(['user', 'assistant', 'system_summary']),
  "content": zod.string().describe('用户可见文本。不包含 Provider 原始响应对象，\n也不包含模型的隐藏思维过程。\n'),
  "status": zod.enum(['draft', 'completed', 'failed', 'superseded']),
  "turn_id": zod.string().nullish(),
  "proposal_ids": zod.array(zod.string()).optional().describe('本条消息附带的待确认建议。'),
  "created_at": zod.string().datetime({"offset":true})
})).describe('按 message_seq 倒序返回，最新的在前。'),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 同步保存用户消息并返回 202 与 operation_id，回复在 Worker 中异步生成。
 * 客户端通过 Operation 轮询，完成后读取消息列表。
 *
 * 想要逐字体验时，可以另外连接
 * `GET /v1/assistant/turns/{turn_id}/stream`（SSE，帧结构见
 * TurnStreamEvent）。它只是传输方式，不是新的权威状态源：
 * 连不上或中途断开时退回轮询即可，不会丢内容。
 * @summary 发送一条消息并触发回复
 */
export const CreateTurnParams = zod.object({
  "thread_id": zod.string()
})

export const createTurnHeaderIdempotencyKeyMin = 8;
export const createTurnHeaderIdempotencyKeyMax = 128;



export const CreateTurnHeader = zod.object({
  "Idempotency-Key": zod.string().min(createTurnHeaderIdempotencyKeyMin).max(createTurnHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const createTurnBodyTextMax = 2000;



export const CreateTurnBody = zod.object({
  "text": zod.string().min(1).max(createTurnBodyTextMax).describe('第一阶段通用 Assistant 只支持文本。\n图片与音频继续走统一 Capture，不在这里重复一套媒体协议。\n'),
  "entry_context": zod.object({
  "screen": zod.string().optional().describe('例如 task_detail、today。'),
  "resource_type": zod.string().nullish(),
  "resource_id": zod.string().nullish(),
  "selected_date": zod.string().date().nullish()
}).optional().describe('客户端的页面上下文，只用于消歧，不是授权。\n服务端会重新校验资源归属与版本；App 不得传入权限结论。\n')
})

export const CreateTurnResponse = zod.object({
  "data": zod.object({
  "thread_id": zod.string(),
  "message_id": zod.string(),
  "turn_id": zod.string(),
  "operation_id": zod.string()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * SSE 长连接。它只是传输方式，不是新的权威状态源：
 * 连不上、中途断开或事件丢失都不影响正确性，客户端读 Message
 * 与 Operation 就能恢复全貌，也可以完全不用它、只轮询 Operation。
 *
 * 服务端实现挂在传输层而不是生成的 strict server 上：
 * 后者给不出可以持续 flush 的写入口。这一条与本地存储的传输端点同理。
 * @summary 订阅回复的实时进度
 */
export const StreamTurnParams = zod.object({
  "turn_id": zod.string()
})

export const StreamTurnResponse = zod.unknown()

/**
 * @summary 取消仍在执行的回复
 */
export const CancelTurnParams = zod.object({
  "turn_id": zod.string()
})

export const cancelTurnHeaderIdempotencyKeyMin = 8;
export const cancelTurnHeaderIdempotencyKeyMax = 128;



export const CancelTurnHeader = zod.object({
  "Idempotency-Key": zod.string().min(cancelTurnHeaderIdempotencyKeyMin).max(cancelTurnHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const CancelTurnResponse = zod.object({
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取待确认与历史建议
 */
export const listProposalsQueryLimitDefault = 20;
export const listProposalsQueryLimitMax = 100;



export const ListProposalsQueryParams = zod.object({
  "status": zod.array(zod.enum(['pending', 'executed', 'rejected', 'expired', 'stale', 'superseded', 'failed'])).optional().describe('不传时只返回待确认项。'),
  "cursor": zod.string().optional().describe('上一页返回的不透明游标。App 不得解析其内容。'),
  "limit": zod.number().int().min(1).max(listProposalsQueryLimitMax).default(listProposalsQueryLimitDefault).describe('单页条数。')
})

export const ListProposalsResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "thread_id": zod.string().nullish(),
  "turn_id": zod.string().nullish(),
  "proposal_type": zod.enum(['task_create', 'task_update', 'task_split', 'event_create', 'event_update', 'memory_upsert']).describe('每种类型都有独立的 command 结构与对应的 Domain Command Mapper。\ncommand 不是任意 JSON Patch。\n'),
  "target_type": zod.string().nullish(),
  "target_id": zod.string().nullish(),
  "target_expected_version": zod.number().int().nullish().describe('目标在建议生成后被改动过时，确认会失败并标记 stale。'),
  "status": zod.enum(['pending', 'executed', 'rejected', 'expired', 'stale', 'superseded', 'failed']),
  "preview": zod.object({
  "title": zod.string().describe('一句话说明这条建议会做什么。'),
  "changes": zod.array(zod.object({
  "field": zod.string(),
  "label": zod.string().describe('面向用户的字段名。'),
  "before": zod.string().nullish(),
  "after": zod.string().nullish()
})).optional(),
  "impact": zod.string().nullish().describe('影响范围说明，例如会影响多少条内容。')
}),
  "reason": zod.string().describe('模型给出的理由。它不能替代服务端的重新校验。'),
  "source_refs": zod.array(zod.string()).optional().describe('生成这条建议时实际读取过的来源。'),
  "editable_fields": zod.array(zod.string()).optional().describe('用户在确认页可以修改的字段。其他字段不接受编辑。'),
  "expires_at": zod.string().datetime({"offset":true}),
  "executed_batch_id": zod.string().nullish().describe('执行成功后生成的 Activity 批次，用于撤销。'),
  "created_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
})),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取建议详情
 */
export const GetProposalParams = zod.object({
  "proposal_id": zod.string()
})

export const GetProposalResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "thread_id": zod.string().nullish(),
  "turn_id": zod.string().nullish(),
  "proposal_type": zod.enum(['task_create', 'task_update', 'task_split', 'event_create', 'event_update', 'memory_upsert']).describe('每种类型都有独立的 command 结构与对应的 Domain Command Mapper。\ncommand 不是任意 JSON Patch。\n'),
  "target_type": zod.string().nullish(),
  "target_id": zod.string().nullish(),
  "target_expected_version": zod.number().int().nullish().describe('目标在建议生成后被改动过时，确认会失败并标记 stale。'),
  "status": zod.enum(['pending', 'executed', 'rejected', 'expired', 'stale', 'superseded', 'failed']),
  "preview": zod.object({
  "title": zod.string().describe('一句话说明这条建议会做什么。'),
  "changes": zod.array(zod.object({
  "field": zod.string(),
  "label": zod.string().describe('面向用户的字段名。'),
  "before": zod.string().nullish(),
  "after": zod.string().nullish()
})).optional(),
  "impact": zod.string().nullish().describe('影响范围说明，例如会影响多少条内容。')
}),
  "reason": zod.string().describe('模型给出的理由。它不能替代服务端的重新校验。'),
  "source_refs": zod.array(zod.string()).optional().describe('生成这条建议时实际读取过的来源。'),
  "editable_fields": zod.array(zod.string()).optional().describe('用户在确认页可以修改的字段。其他字段不接受编辑。'),
  "expires_at": zod.string().datetime({"offset":true}),
  "executed_batch_id": zod.string().nullish().describe('执行成功后生成的 Activity 批次，用于撤销。'),
  "created_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 服务端会重新读取目标、重新执行权限与状态机校验，再把允许的用户编辑
 * 映射成类型化 Domain Command 执行。模型给出的理由不能替代这一步。
 * 目标在建议生成后被改动过时返回 AI_PROPOSAL_STALE。
 * @summary 确认并执行建议
 */
export const ConfirmProposalParams = zod.object({
  "proposal_id": zod.string()
})

export const confirmProposalHeaderIdempotencyKeyMin = 8;
export const confirmProposalHeaderIdempotencyKeyMax = 128;



export const ConfirmProposalHeader = zod.object({
  "Idempotency-Key": zod.string().min(confirmProposalHeaderIdempotencyKeyMin).max(confirmProposalHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const ConfirmProposalBody = zod.object({
  "proposal_version": zod.number().int().describe('与服务端当前版本不一致时拒绝执行。'),
  "target_expected_version": zod.number().int().nullish().describe('目标资源的期望版本，缺省时使用建议生成时记录的版本。'),
  "edits": zod.record(zod.string(), zod.unknown()).optional().describe('用户在确认页的修改，只允许 editable_fields 声明过的字段。\n不接受替换 command 类型或目标 ID。\n')
})

export const ConfirmProposalResponse = zod.object({
  "data": zod.object({
  "proposal": zod.object({
  "id": zod.string(),
  "thread_id": zod.string().nullish(),
  "turn_id": zod.string().nullish(),
  "proposal_type": zod.enum(['task_create', 'task_update', 'task_split', 'event_create', 'event_update', 'memory_upsert']).describe('每种类型都有独立的 command 结构与对应的 Domain Command Mapper。\ncommand 不是任意 JSON Patch。\n'),
  "target_type": zod.string().nullish(),
  "target_id": zod.string().nullish(),
  "target_expected_version": zod.number().int().nullish().describe('目标在建议生成后被改动过时，确认会失败并标记 stale。'),
  "status": zod.enum(['pending', 'executed', 'rejected', 'expired', 'stale', 'superseded', 'failed']),
  "preview": zod.object({
  "title": zod.string().describe('一句话说明这条建议会做什么。'),
  "changes": zod.array(zod.object({
  "field": zod.string(),
  "label": zod.string().describe('面向用户的字段名。'),
  "before": zod.string().nullish(),
  "after": zod.string().nullish()
})).optional(),
  "impact": zod.string().nullish().describe('影响范围说明，例如会影响多少条内容。')
}),
  "reason": zod.string().describe('模型给出的理由。它不能替代服务端的重新校验。'),
  "source_refs": zod.array(zod.string()).optional().describe('生成这条建议时实际读取过的来源。'),
  "editable_fields": zod.array(zod.string()).optional().describe('用户在确认页可以修改的字段。其他字段不接受编辑。'),
  "expires_at": zod.string().datetime({"offset":true}),
  "executed_batch_id": zod.string().nullish().describe('执行成功后生成的 Activity 批次，用于撤销。'),
  "created_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),
  "affected_resources": zod.array(zod.object({
  "type": zod.enum(['task', 'event', 'project', 'note', 'record', 'tracker', 'task_list', 'capture', 'today', 'calendar', 'activity', 'assistant_thread', 'action_proposal', 'memory', 'memory_moment', 'person', 'person_interaction', 'recipe', 'meal_plan', 'diet_profile']).describe('受影响资源类型，App 据此精确失效缓存。'),
  "id": zod.string().nullable().describe('为空表示该类型的集合查询整体失效。')
})),
  "activity_batch_id": zod.string().nullish()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 不修改任何业务数据。
 * @summary 拒绝建议
 */
export const RejectProposalParams = zod.object({
  "proposal_id": zod.string()
})

export const rejectProposalHeaderIdempotencyKeyMin = 8;
export const rejectProposalHeaderIdempotencyKeyMax = 128;



export const RejectProposalHeader = zod.object({
  "Idempotency-Key": zod.string().min(rejectProposalHeaderIdempotencyKeyMin).max(rejectProposalHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const RejectProposalResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "thread_id": zod.string().nullish(),
  "turn_id": zod.string().nullish(),
  "proposal_type": zod.enum(['task_create', 'task_update', 'task_split', 'event_create', 'event_update', 'memory_upsert']).describe('每种类型都有独立的 command 结构与对应的 Domain Command Mapper。\ncommand 不是任意 JSON Patch。\n'),
  "target_type": zod.string().nullish(),
  "target_id": zod.string().nullish(),
  "target_expected_version": zod.number().int().nullish().describe('目标在建议生成后被改动过时，确认会失败并标记 stale。'),
  "status": zod.enum(['pending', 'executed', 'rejected', 'expired', 'stale', 'superseded', 'failed']),
  "preview": zod.object({
  "title": zod.string().describe('一句话说明这条建议会做什么。'),
  "changes": zod.array(zod.object({
  "field": zod.string(),
  "label": zod.string().describe('面向用户的字段名。'),
  "before": zod.string().nullish(),
  "after": zod.string().nullish()
})).optional(),
  "impact": zod.string().nullish().describe('影响范围说明，例如会影响多少条内容。')
}),
  "reason": zod.string().describe('模型给出的理由。它不能替代服务端的重新校验。'),
  "source_refs": zod.array(zod.string()).optional().describe('生成这条建议时实际读取过的来源。'),
  "editable_fields": zod.array(zod.string()).optional().describe('用户在确认页可以修改的字段。其他字段不接受编辑。'),
  "expires_at": zod.string().datetime({"offset":true}),
  "executed_batch_id": zod.string().nullish().describe('执行成功后生成的 Activity 批次，用于撤销。'),
  "created_at": zod.string().datetime({"offset":true}),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})
