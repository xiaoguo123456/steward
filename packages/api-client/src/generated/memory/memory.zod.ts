/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * 用户据此了解「系统记住了什么、从哪里来、为什么使用」。
 * 每条记忆都带来源，没有来源的条目不会存在。
 * @summary 查看系统记住了什么
 */
export const listMemoriesQueryLimitDefault = 20;
export const listMemoriesQueryLimitMax = 100;



export const ListMemoriesQueryParams = zod.object({
  "memory_type": zod.enum(['communication_preference', 'routine_preference', 'domain_preference', 'personal_context', 'constraint']).optional(),
  "status": zod.array(zod.enum(['active', 'superseded', 'deleted', 'expired', 'shadowed'])).optional().describe('不传时只返回生效中的记忆。'),
  "cursor": zod.string().optional().describe('上一页返回的不透明游标。App 不得解析其内容。'),
  "limit": zod.number().int().min(1).max(listMemoriesQueryLimitMax).default(listMemoriesQueryLimitDefault).describe('单页条数。')
})

export const listMemoriesResponseDataItemEvidenceItemSourceDeletedDefault = false;

export const ListMemoriesResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "memory_key": zod.string().describe('稳定语义键，例如 communication.response_style。'),
  "memory_type": zod.enum(['communication_preference', 'routine_preference', 'domain_preference', 'personal_context', 'constraint']).describe('第一阶段只开放这几种稳定类型。\n禁止用一个自由文本类型无限承载未知业务。\n'),
  "canonical_text": zod.string().describe('面向用户展示的规范表述。'),
  "sensitivity": zod.enum(['normal', 'sensitive', 'highly_sensitive']).describe('健康、财务、住址、家庭关系与受保护属性默认视为高敏。\n高敏记忆只允许用户明确输入并单独确认，不通过行为自动推断。\n'),
  "origin": zod.enum(['explicit', 'learned', 'imported']).describe('explicit 表示用户直接说出；learned 表示从对话中学到并经确认。'),
  "status": zod.enum(['active', 'superseded', 'deleted', 'expired', 'shadowed']),
  "evidence": zod.array(zod.object({
  "source_type": zod.enum(['assistant_message', 'user_message', 'object', 'record', 'user_setting']),
  "source_id": zod.string(),
  "source_deleted": zod.boolean().default(listMemoriesResponseDataItemEvidenceItemSourceDeletedDefault).describe('来源被删除后显示 tombstone，不把原文复制回记忆。'),
  "evidence_role": zod.enum(['explicit', 'inferred', 'user_confirmed']),
  "created_at": zod.string().datetime({"offset":true})
})).optional().describe('每条记忆至少有一条来源。用户可以据此判断系统为什么记住它。'),
  "confirmed_at": zod.string().datetime({"offset":true}).nullish(),
  "last_used_at": zod.string().datetime({"offset":true}).nullish().describe('最近一次被用作上下文的时间。被引用不提高事实可信度。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}).optional(),
  "version": zod.number().int()
})),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 查看记忆的值、来源与历史
 */
export const GetMemoryParams = zod.object({
  "memory_id": zod.string()
})

export const getMemoryResponseDataEvidenceItemSourceDeletedDefault = false;

export const GetMemoryResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "memory_key": zod.string().describe('稳定语义键，例如 communication.response_style。'),
  "memory_type": zod.enum(['communication_preference', 'routine_preference', 'domain_preference', 'personal_context', 'constraint']).describe('第一阶段只开放这几种稳定类型。\n禁止用一个自由文本类型无限承载未知业务。\n'),
  "canonical_text": zod.string().describe('面向用户展示的规范表述。'),
  "sensitivity": zod.enum(['normal', 'sensitive', 'highly_sensitive']).describe('健康、财务、住址、家庭关系与受保护属性默认视为高敏。\n高敏记忆只允许用户明确输入并单独确认，不通过行为自动推断。\n'),
  "origin": zod.enum(['explicit', 'learned', 'imported']).describe('explicit 表示用户直接说出；learned 表示从对话中学到并经确认。'),
  "status": zod.enum(['active', 'superseded', 'deleted', 'expired', 'shadowed']),
  "evidence": zod.array(zod.object({
  "source_type": zod.enum(['assistant_message', 'user_message', 'object', 'record', 'user_setting']),
  "source_id": zod.string(),
  "source_deleted": zod.boolean().default(getMemoryResponseDataEvidenceItemSourceDeletedDefault).describe('来源被删除后显示 tombstone，不把原文复制回记忆。'),
  "evidence_role": zod.enum(['explicit', 'inferred', 'user_confirmed']),
  "created_at": zod.string().datetime({"offset":true})
})).optional().describe('每条记忆至少有一条来源。用户可以据此判断系统为什么记住它。'),
  "confirmed_at": zod.string().datetime({"offset":true}).nullish(),
  "last_used_at": zod.string().datetime({"offset":true}).nullish().describe('最近一次被用作上下文的时间。被引用不提高事实可信度。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}).optional(),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 修改会创建新的 Revision，旧值保留最小审计关系但不再使用。
 * @summary 修改已确认的记忆
 */
export const UpdateMemoryParams = zod.object({
  "memory_id": zod.string()
})

export const UpdateMemoryHeader = zod.object({
  "If-Match": zod.string().optional().describe('目标资源的 version。提供后与服务端当前版本不一致时返回 VERSION_CONFLICT。\n并发编辑场景应始终携带。\n')
})

export const updateMemoryBodyCanonicalTextMax = 200;



export const UpdateMemoryBody = zod.object({
  "canonical_text": zod.string().min(1).max(updateMemoryBodyCanonicalTextMax).describe('用户改写后的表述。修改会创建新的 Revision。')
})

export const updateMemoryResponseDataEvidenceItemSourceDeletedDefault = false;

export const UpdateMemoryResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "memory_key": zod.string().describe('稳定语义键，例如 communication.response_style。'),
  "memory_type": zod.enum(['communication_preference', 'routine_preference', 'domain_preference', 'personal_context', 'constraint']).describe('第一阶段只开放这几种稳定类型。\n禁止用一个自由文本类型无限承载未知业务。\n'),
  "canonical_text": zod.string().describe('面向用户展示的规范表述。'),
  "sensitivity": zod.enum(['normal', 'sensitive', 'highly_sensitive']).describe('健康、财务、住址、家庭关系与受保护属性默认视为高敏。\n高敏记忆只允许用户明确输入并单独确认，不通过行为自动推断。\n'),
  "origin": zod.enum(['explicit', 'learned', 'imported']).describe('explicit 表示用户直接说出；learned 表示从对话中学到并经确认。'),
  "status": zod.enum(['active', 'superseded', 'deleted', 'expired', 'shadowed']),
  "evidence": zod.array(zod.object({
  "source_type": zod.enum(['assistant_message', 'user_message', 'object', 'record', 'user_setting']),
  "source_id": zod.string(),
  "source_deleted": zod.boolean().default(updateMemoryResponseDataEvidenceItemSourceDeletedDefault).describe('来源被删除后显示 tombstone，不把原文复制回记忆。'),
  "evidence_role": zod.enum(['explicit', 'inferred', 'user_confirmed']),
  "created_at": zod.string().datetime({"offset":true})
})).optional().describe('每条记忆至少有一条来源。用户可以据此判断系统为什么记住它。'),
  "confirmed_at": zod.string().datetime({"offset":true}).nullish(),
  "last_used_at": zod.string().datetime({"offset":true}).nullish().describe('最近一次被用作上下文的时间。被引用不提高事实可信度。'),
  "created_at": zod.string().datetime({"offset":true}),
  "updated_at": zod.string().datetime({"offset":true}).optional(),
  "version": zod.number().int()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 删除后立即从检索与 Prompt 上下文中排除。
 * 选择阻止重新学习时写入 Relearn Block，只保存指纹，不保存明文。
 * @summary 删除记忆
 */
export const DeleteMemoryParams = zod.object({
  "memory_id": zod.string()
})

export const deleteMemoryHeaderIdempotencyKeyMin = 8;
export const deleteMemoryHeaderIdempotencyKeyMax = 128;



export const DeleteMemoryHeader = zod.object({
  "Idempotency-Key": zod.string().min(deleteMemoryHeaderIdempotencyKeyMin).max(deleteMemoryHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const deleteMemoryBodyBlockRelearningDefault = false;

export const DeleteMemoryBody = zod.object({
  "block_relearning": zod.boolean().default(deleteMemoryBodyBlockRelearningDefault).describe('为 true 时同时写入 Relearn Block，阻止系统再次自动学到同一语义。\nBlock 只保存指纹，不保存明文，也不能用来恢复原值。\n')
})

export const DeleteMemoryResponse = zod.object({
  "data": zod.object({
  "recoverable": zod.boolean().describe('是否可以恢复。高敏记忆不提供恢复，契约显式给出该字段，\n不让 App 自行猜测。\n'),
  "blocked_relearning": zod.boolean().optional()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 查看已禁止重新学习的项目
 */
export const ListRelearnBlocksResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "memory_key": zod.string().describe('只展示语义键，不展示被阻止的具体内容。'),
  "blocked_at": zod.string().datetime({"offset":true})
})),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 解除重新学习阻止
 */
export const DeleteRelearnBlockParams = zod.object({
  "block_id": zod.string()
})

export const deleteRelearnBlockHeaderIdempotencyKeyMin = 8;
export const deleteRelearnBlockHeaderIdempotencyKeyMax = 128;



export const DeleteRelearnBlockHeader = zod.object({
  "Idempotency-Key": zod.string().min(deleteRelearnBlockHeaderIdempotencyKeyMin).max(deleteRelearnBlockHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const DeleteRelearnBlockResponse = zod.object({
  "data": zod.object({
  "affected_resources": zod.array(zod.object({
  "type": zod.enum(['task', 'event', 'project', 'note', 'record', 'tracker', 'task_list', 'capture', 'today', 'calendar', 'activity', 'assistant_thread', 'action_proposal', 'memory', 'memory_moment', 'person', 'person_interaction', 'recipe', 'meal_plan', 'diet_profile']).describe('受影响资源类型，App 据此精确失效缓存。'),
  "id": zod.string().nullable().describe('为空表示该类型的集合查询整体失效。')
})),
  "activity_batch_id": zod.string().nullish()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
}).describe('删除等不返回实体的写操作响应，携带需要失效的资源列表。')

