/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { ConfirmProposalRequestEdits } from './confirmProposalRequestEdits';

export interface ConfirmProposalRequest {
  /** 与服务端当前版本不一致时拒绝执行。 */
  proposal_version: number;
  /**
     * 目标资源的期望版本，缺省时使用建议生成时记录的版本。
     * @nullable
     */
  target_expected_version?: number | null;
  /**
     * 用户在确认页的修改，只允许 editable_fields 声明过的字段。
     * 不接受替换 command 类型或目标 ID。
     */
  edits?: ConfirmProposalRequestEdits;
}
