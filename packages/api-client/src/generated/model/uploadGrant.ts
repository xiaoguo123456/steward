/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { UploadGrantHeaders } from './uploadGrantHeaders';
import type { UploadGrantMethod } from './uploadGrantMethod';

export interface UploadGrant {
  media_id: string;
  /**
     * 短期直传地址。客户端按 method 发送字节并带上 headers 即可，
     * 不需要知道背后是哪家对象存储。该地址不得写入日志或分享。
     */
  upload_url: string;
  method: UploadGrantMethod;
  /** 上传时必须原样携带的请求头，其中 Content-Type 参与签名。 */
  headers?: UploadGrantHeaders;
  /** 该类型允许的最大字节数，超出时上传会被拒绝。 */
  max_bytes?: number;
  /** 过期后需要重新申请授权，但仍复用同一个 media_id。 */
  expires_at: string;
}
