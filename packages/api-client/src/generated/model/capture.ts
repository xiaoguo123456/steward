/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { AffectedResource } from './affectedResource';
import type { CaptureCandidate } from './captureCandidate';
import type { CaptureConflict } from './captureConflict';
import type { CaptureOrigin } from './captureOrigin';
import type { CapturePart } from './capturePart';
import type { CaptureQuestion } from './captureQuestion';
import type { CaptureRelationCandidate } from './captureRelationCandidate';
import type { CaptureStatus } from './captureStatus';
import type { ErrorBody } from './errorBody';

export interface Capture {
  id: string;
  status: CaptureStatus;
  /** 当前最新 revision。只允许确认最新 revision。 */
  revision: number;
  origin: CaptureOrigin;
  parts: CapturePart[];
  candidates: CaptureCandidate[];
  relation_candidates?: CaptureRelationCandidate[];
  questions?: CaptureQuestion[];
  conflicts?: CaptureConflict[];
  /**
     * 用户在文字或语音中给出的限制条件，例如“只处理第一张”。
     * @nullable
     */
  instruction_note?: string | null;
  error?: ErrorBody;
  /** 确认后本次实际创建或更新的实体引用。 */
  created_objects?: AffectedResource[];
  /**
     * 确认成功后生成的 Activity 批次，用于即时撤销。
     * @nullable
     */
  activity_batch_id?: string | null;
  created_at: string;
  updated_at: string;
  /** @nullable */
  confirmed_at?: string | null;
}
