/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { NotificationSourceType } from './notificationSourceType';
import type { NotificationType } from './notificationType';

export interface Notification {
  id: string;
  type: NotificationType;
  /** 触发时保存的最小标题快照；来源删除后仍可解释这条通知。 */
  title: string;
  /** 不包含用户正文的确定性通知说明。 */
  body: string;
  source_type: NotificationSourceType;
  source_id: string;
  occurred_at: string;
  /** @nullable */
  read_at: string | null;
  /** 为 true 时客户端显示“内容已删除”，不得进入空白详情。 */
  source_deleted: boolean;
}
