/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */

/**
 * 内容来源与授权。字段不全的菜谱不允许进库：
 * 不知道来源和授权范围，就等于不知道自己有没有权利展示它。
 */
export interface RecipeSource {
  /** 内容提供方。 */
  name: string;
  /** @nullable */
  author?: string | null;
  /**
     * 原始出处。
     * @nullable
     */
  url?: string | null;
  /** 授权范围，例如 CC-BY-4.0、平台自有内容。 */
  license: string;
  /** @nullable */
  license_url?: string | null;
  /**
     * 图片的权利说明。没有明确权利的图片不下发 image_url。
     * @nullable
     */
  image_credit?: string | null;
  /** 内容版本。来源更新时递增，客户端据此判断缓存是否过期。 */
  content_version: string;
}
