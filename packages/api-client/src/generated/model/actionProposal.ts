/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { ProposalPreview } from './proposalPreview';
import type { ProposalStatus } from './proposalStatus';
import type { ProposalType } from './proposalType';

export interface ActionProposal {
  id: string;
  /** @nullable */
  thread_id?: string | null;
  /** @nullable */
  turn_id?: string | null;
  proposal_type: ProposalType;
  /** @nullable */
  target_type?: string | null;
  /** @nullable */
  target_id?: string | null;
  /**
     * 目标在建议生成后被改动过时，确认会失败并标记 stale。
     * @nullable
     */
  target_expected_version?: number | null;
  status: ProposalStatus;
  preview: ProposalPreview;
  /** 模型给出的理由。它不能替代服务端的重新校验。 */
  reason: string;
  /** 生成这条建议时实际读取过的来源。 */
  source_refs?: string[];
  /** 用户在确认页可以修改的字段。其他字段不接受编辑。 */
  editable_fields?: string[];
  expires_at: string;
  /**
     * 执行成功后生成的 Activity 批次，用于撤销。
     * @nullable
     */
  executed_batch_id?: string | null;
  created_at: string;
  version: number;
}
