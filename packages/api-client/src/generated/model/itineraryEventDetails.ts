/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { BookingStatus } from './bookingStatus';
import type { ItineraryItemKind } from './itineraryItemKind';
import type { TransportMode } from './transportMode';

export interface ItineraryEventDetails {
  kind: ItineraryItemKind;
  transport_mode?: TransportMode;
  /**
     * @maxLength 200
     * @nullable
     */
  origin?: string | null;
  /**
     * @maxLength 200
     * @nullable
     */
  destination?: string | null;
  /**
     * 航班号、车次、船次等承运班次。
     * @maxLength 50
     * @nullable
     */
  service_number?: string | null;
  /**
     * 座位、车厢或舱位；列表页不展示票号等敏感信息。
     * @maxLength 100
     * @nullable
     */
  seat?: string | null;
  booking_status: BookingStatus;
  /**
     * 经用户确认保留的票据图片媒体 ID；读取时再换取短期地址。
     * @maxItems 9
     */
  attachment_media_ids: string[];
}
