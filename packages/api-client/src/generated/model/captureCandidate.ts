/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { CaptureCandidateAction } from './captureCandidateAction';
import type { CaptureCandidateType } from './captureCandidateType';
import type { CaptureDraftPayload } from './captureDraftPayload';
import type { CaptureFieldConfidence } from './captureFieldConfidence';
import type { CaptureSourceRef } from './captureSourceRef';

export interface CaptureCandidate {
  id: string;
  candidate_type: CaptureCandidateType;
  action: CaptureCandidateAction;
  /**
     * action=update 时指向已有实体。
     * @nullable
     */
  target_id?: string | null;
  /** @nullable */
  target_expected_version?: number | null;
  /** 服务端给出的默认勾选状态，用户可以修改。 */
  selected: boolean;
  payload: CaptureDraftPayload;
  field_confidences?: CaptureFieldConfidence[];
  source_refs?: CaptureSourceRef[];
  /** 未解决的必填字段。非空时不允许保存该候选项。 */
  missing_fields?: string[];
  warnings?: string[];
  /**
     * 服务端确定性重复检测命中的已有实体 ID。
     * @nullable
     */
  duplicate_of?: string | null;
}
