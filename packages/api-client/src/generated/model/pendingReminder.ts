/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { EventKind } from './eventKind';
import type { PendingReminderSourceType } from './pendingReminderSourceType';

/**
 * 一条到点了、用户还没处理的提醒。
 *
 * 它不是一张表里的行：提醒是事项上的一条规则，加上一次「发生」算出来的时刻。
 * 因此 id 是复合键（来源 ID｜规则 ID｜这次发生的日期），消掉时原样回传。
 */
export interface PendingReminder {
  /** 复合键，消掉这条提醒时原样回传。 */
  id: string;
  source_type: PendingReminderSourceType;
  /** 点击后跳转到这个事项。 */
  source_id: string;
  title: string;
  /** 这条提醒该响的时刻。可能已经过去，客户端据此显示「今天」或具体日期。 */
  fire_at: string;
  /** 这一次发生的日期。重要日每年重复，靠它区分是哪一年那次。 */
  occurrence_date: string;
  event_kind?: EventKind;
}
