/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { CaptureQuestionStatus } from './captureQuestionStatus';

export interface CaptureQuestion {
  id: string;
  capture_id: string;
  /** 面向用户的中文追问，说明为什么需要这项信息。 */
  question: string;
  /** 为 true 时 Capture 处于 awaiting_instruction，解析被阻塞。 */
  blocking: boolean;
  status: CaptureQuestionStatus;
  /** 可直接点击的快捷回答，仍需生成新 revision 重新解析。 */
  quick_answers?: string[];
  /** 用于全局待答入口列表展示的一句话摘要。 */
  capture_summary?: string;
  created_at: string;
  /** @nullable */
  answered_at?: string | null;
}
