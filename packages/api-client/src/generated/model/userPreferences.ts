/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { UserPreferencesWeekStart } from './userPreferencesWeekStart';

export interface UserPreferences {
  /** 周视图起始日。 */
  week_start: UserPreferencesWeekStart;
  /** 工作时间开始，格式 HH:MM，用于安排建议与日历高亮。 */
  work_day_start: string;
  work_day_end: string;
  /** 全天 Event 与 due_date 提醒的默认当地触发时间。 */
  default_reminder_local_time: string;
  updated_at: string;
}
