/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { ProvenanceAction } from './provenanceAction';
import type { ProvenanceRefSourceType } from './provenanceRefSourceType';

export interface ProvenanceRef {
  source_type: ProvenanceRefSourceType;
  source_id: string;
  /**
     * Capture 来源的 revision 序号。
     * @nullable
     */
  source_revision?: number | null;
  /** Capture 中的文字片段、音频时间段或图片区域引用。 */
  part_refs?: string[];
  action: ProvenanceAction;
  /** 来源已删除时为 true，此时不再保留原始内容。 */
  source_deleted?: boolean;
}
