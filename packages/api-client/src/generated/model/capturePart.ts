/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { CapturePartKind } from './capturePartKind';
import type { CapturePartStatus } from './capturePartStatus';
import type { ErrorBody } from './errorBody';

export interface CapturePart {
  id: string;
  kind: CapturePartKind;
  status: CapturePartStatus;
  /** 提交顺序，图片按此编号显示为“图片 1、图片 2”。 */
  position: number;
  /**
     * kind=text 时为用户输入原文；kind=audio 时为转写结果；kind=image 时为 OCR 摘要。
     * @nullable
     */
  text?: string | null;
  /**
     * 有效期受限的私有访问地址。
     * @nullable
     */
  media_url?: string | null;
  /** @nullable */
  duration_ms?: number | null;
  error?: ErrorBody;
}
