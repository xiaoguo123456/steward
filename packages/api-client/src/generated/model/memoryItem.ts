/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { MemoryEvidence } from './memoryEvidence';
import type { MemoryOrigin } from './memoryOrigin';
import type { MemorySensitivity } from './memorySensitivity';
import type { MemoryStatus } from './memoryStatus';
import type { MemoryType } from './memoryType';

export interface MemoryItem {
  id: string;
  /** 稳定语义键，例如 communication.response_style。 */
  memory_key: string;
  memory_type: MemoryType;
  /** 面向用户展示的规范表述。 */
  canonical_text: string;
  sensitivity: MemorySensitivity;
  origin: MemoryOrigin;
  status: MemoryStatus;
  /** 每条记忆至少有一条来源。用户可以据此判断系统为什么记住它。 */
  evidence?: MemoryEvidence[];
  /** @nullable */
  confirmed_at?: string | null;
  /**
     * 最近一次被用作上下文的时间。被引用不提高事实可信度。
     * @nullable
     */
  last_used_at?: string | null;
  created_at: string;
  updated_at?: string;
  version: number;
}
