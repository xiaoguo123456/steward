/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { ConfirmCaptureItem } from './confirmCaptureItem';

export interface ConfirmCaptureRequest {
  /** 必须等于当前最新 revision，否则返回 CAPTURE_REVISION_STALE。 */
  revision: number;
  /**
     * 用户勾选并确认保存的候选项。未列出的候选视为放弃。
     * @minItems 1
     */
  items: ConfirmCaptureItem[];
}
