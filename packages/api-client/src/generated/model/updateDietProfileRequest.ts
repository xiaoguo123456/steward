/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import type { DietActivityLevel } from './dietActivityLevel';
import type { DietGoal } from './dietGoal';
import type { DietSex } from './dietSex';
import type { UpdateDietProfileRequestClearItem } from './updateDietProfileRequestClearItem';

/**
 * 只提交需要修改的字段；不传表示保持原值。
 * 清空一个可空字段必须把字段名放进 clear 数组，
 * 因为生成的 Go 类型无法区分“不传”与“传 null”。
 */
export interface UpdateDietProfileRequest {
  clear?: UpdateDietProfileRequestClearItem[];
  goal?: DietGoal;
  /**
     * @minimum 1
     * @maximum 120
     * @nullable
     */
  age?: number | null;
  sex?: DietSex;
  /**
     * @minimum 50
     * @maximum 260
     * @nullable
     */
  height_cm?: number | null;
  /**
     * @minimum 20
     * @maximum 400
     * @nullable
     */
  weight_kg?: number | null;
  /**
     * @minimum 20
     * @maximum 400
     * @nullable
     */
  target_weight_kg?: number | null;
  activity_level?: DietActivityLevel;
  allergens?: string[];
  dislikes?: string[];
  /**
     * @minimum 1
     * @maximum 20
     */
  servings?: number;
  /**
     * @minimum 1
     * @nullable
     */
  max_cook_minutes?: number | null;
  completed?: boolean;
}
