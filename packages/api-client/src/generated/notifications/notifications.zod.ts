/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事 API
 * 序事的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * 返回已经触发并持久化的通知，按发生时间倒序排列。当前 Event Reminder
 * 与 Task Due 复用服务端 Reminder 的确定性触发计算；系统通知权限被拒绝
 * 不影响本接口。来源软删除后仍返回通知快照，并标记 source_deleted。
 * @summary 读取产品内通知
 */
export const listNotificationsQueryLimitDefault = 20;
export const listNotificationsQueryLimitMax = 100;



export const ListNotificationsQueryParams = zod.object({
  "cursor": zod.string().optional().describe('上一页返回的不透明游标。App 不得解析其内容。'),
  "limit": zod.number().int().min(1).max(listNotificationsQueryLimitMax).default(listNotificationsQueryLimitDefault).describe('单页条数。')
})

export const ListNotificationsResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "type": zod.enum(['event_reminder', 'task_due', 'daily_brief', 'project_risk', 'weekly_review']),
  "title": zod.string().describe('触发时保存的最小标题快照；来源删除后仍可解释这条通知。'),
  "body": zod.string().describe('不包含用户正文的确定性通知说明。'),
  "source_type": zod.enum(['event', 'task', 'project', 'review']),
  "source_id": zod.string(),
  "occurred_at": zod.string().datetime({"offset":true}),
  "read_at": zod.string().datetime({"offset":true}).nullable(),
  "source_deleted": zod.boolean().describe('为 true 时客户端显示“内容已删除”，不得进入空白详情。')
})),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 重复标记同一条通知不会改变首次已读时间。
 * @summary 标记一条产品内通知已读
 */
export const MarkNotificationReadParams = zod.object({
  "notification_id": zod.string()
})

export const markNotificationReadHeaderIdempotencyKeyMin = 8;
export const markNotificationReadHeaderIdempotencyKeyMax = 128;



export const MarkNotificationReadHeader = zod.object({
  "Idempotency-Key": zod.string().min(markNotificationReadHeaderIdempotencyKeyMin).max(markNotificationReadHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const MarkNotificationReadResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "type": zod.enum(['event_reminder', 'task_due', 'daily_brief', 'project_risk', 'weekly_review']),
  "title": zod.string().describe('触发时保存的最小标题快照；来源删除后仍可解释这条通知。'),
  "body": zod.string().describe('不包含用户正文的确定性通知说明。'),
  "source_type": zod.enum(['event', 'task', 'project', 'review']),
  "source_id": zod.string(),
  "occurred_at": zod.string().datetime({"offset":true}),
  "read_at": zod.string().datetime({"offset":true}).nullable(),
  "source_deleted": zod.boolean().describe('为 true 时客户端显示“内容已删除”，不得进入空白详情。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})
