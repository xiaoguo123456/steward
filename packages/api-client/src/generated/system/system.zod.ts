/**
 * Generated by orval 🍺
 * Do not edit manually.
 * AI事管家 API
 * AI事管家的唯一网络契约来源。前后端不得在任一侧手写 DTO、URL、枚举或错误码。
 *
 * 约定：
 * - 所有成功响应统一为 `{data, meta}`，集合额外带 `page`。
 * - 所有错误响应统一为 `{error, meta}`，App 只依赖 `error.code` 分支。
 * - 写请求必须携带 `Idempotency-Key`；并发编辑应携带 `If-Match`。
 * - 异步处理返回 `202` 与 `operation_id`，客户端通过 Operation 轮询。
 * - 私有内容默认 `Cache-Control: private, no-store`。
 *
 * OpenAPI spec version: 1.0.0
 */
import * as zod from 'zod';


/**
 * Operation 只返回结果引用，不内嵌完整实体正文，
 * 以免幂等快照、日志与轮询响应复制用户正文。
 * @summary 读取异步任务状态
 */
export const GetOperationParams = zod.object({
  "operation_id": zod.string()
})

export const getOperationResponseDataErrorReloadTargetDefault = false;

export const GetOperationResponse = zod.object({
  "data": zod.object({
  "id": zod.string(),
  "kind": zod.string().describe('异步任务类型，例如 capture.parse。'),
  "status": zod.enum(['queued', 'running', 'succeeded', 'failed', 'cancelled']),
  "progress": zod.number().int().nullish().describe('0 到 100 的粗粒度进度，仅用于展示。'),
  "result_ref": zod.record(zod.string(), zod.unknown()).nullish().describe('指向结果资源的最小引用，不内嵌完整实体正文。\n例如 {\"type\":\"capture\",\"capture_id\":\"cap_...\",\"revision\":1}。\n'),
  "error": zod.object({
  "code": zod.enum(['VALIDATION_FAILED', 'UNAUTHENTICATED', 'PERMISSION_DENIED', 'RESOURCE_NOT_FOUND', 'VERSION_CONFLICT', 'IDEMPOTENCY_KEY_REQUIRED', 'IDEMPOTENCY_KEY_REUSED', 'RATE_LIMITED', 'INTERNAL_ERROR', 'PHONE_INVALID', 'PHONE_ALREADY_IN_USE', 'VERIFICATION_CODE_INVALID', 'VERIFICATION_CODE_EXPIRED', 'REFRESH_TOKEN_INVALID', 'SMS_PROVIDER_UNAVAILABLE', 'ACCOUNT_NOT_ACTIVE', 'REAUTH_TOKEN_INVALID', 'REAUTH_TOKEN_EXPIRED', 'ACCOUNT_DELETION_ALREADY_REQUESTED', 'ACCOUNT_DELETION_STATUS_TOKEN_INVALID', 'TASK_LIST_NAME_DUPLICATED', 'TASK_LIST_NOT_EMPTY', 'TASK_LIST_DEFAULT_REQUIRED', 'TASK_STATUS_TRANSITION_INVALID', 'EVENT_TIME_RANGE_INVALID', 'EVENT_RECURRENCE_NOT_ALLOWED', 'TRACKER_SCHEMA_INVALID', 'RECORD_VALUES_INVALID', 'PROJECT_HAS_OPEN_TASKS', 'OBJECT_ALREADY_DELETED', 'CAPTURE_NOT_READY', 'CAPTURE_ALREADY_CONFIRMED', 'CAPTURE_REVISION_STALE', 'CAPTURE_QUESTION_RESOLVED', 'AI_PROVIDER_UNAVAILABLE', 'AI_PROVIDER_RATE_LIMITED', 'AI_SUGGESTION_DISABLED', 'AI_SCHEMA_INVALID', 'AI_SOURCE_INVALID', 'AI_BUDGET_EXCEEDED', 'AI_TURN_CANCELLED', 'AI_PROPOSAL_STALE', 'AI_PROPOSAL_EXPIRED', 'AI_PROPOSAL_ALREADY_RESOLVED', 'AI_PROPOSAL_EDIT_NOT_ALLOWED', 'MEMORY_RELEARN_BLOCKED']).describe('稳定错误码。App 只能依赖此枚举做分支，不得解析 message 文案。\n新增错误码必须同时定义默认中文文案、是否可重试和推荐 App 动作。\n'),
  "message": zod.string().describe('面向用户的默认中文文案。App 可以覆盖为更贴合场景的文案。'),
  "retryable": zod.boolean().describe('是否可以用相同参数安全重试。'),
  "reload_target": zod.boolean().default(getOperationResponseDataErrorReloadTargetDefault).describe('App 是否需要重新加载目标资源后再重试。'),
  "details": zod.array(zod.object({
  "field": zod.string().describe('出错字段的路径，例如 scheduled_end_at。'),
  "message": zod.string()
})).optional().describe('字段级校验明细，仅在 VALIDATION_FAILED 等场景出现。')
}).optional(),
  "created_at": zod.string().datetime({"offset":true}),
  "completed_at": zod.string().datetime({"offset":true}).nullish()
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * @summary 读取变更历史
 */
export const listActivityBatchesQueryLimitDefault = 20;
export const listActivityBatchesQueryLimitMax = 100;



export const ListActivityBatchesQueryParams = zod.object({
  "cursor": zod.string().optional().describe('上一页返回的不透明游标。App 不得解析其内容。'),
  "limit": zod.number().int().min(1).max(listActivityBatchesQueryLimitMax).default(listActivityBatchesQueryLimitDefault).describe('单页条数。')
})

export const ListActivityBatchesResponse = zod.object({
  "data": zod.array(zod.object({
  "id": zod.string(),
  "source": zod.enum(['user_form', 'capture_confirm', 'assistant_proposal', 'system']).describe('触发本批次的来源。'),
  "source_id": zod.string().nullish().describe('例如触发本批次的 Capture ID。'),
  "entries": zod.array(zod.object({
  "id": zod.string(),
  "action": zod.enum(['created', 'updated', 'deleted', 'restored', 'completed', 'reopened']),
  "resource_type": zod.string(),
  "resource_id": zod.string(),
  "title": zod.string().optional().describe('变更时的实体标题快照，用于列表展示。'),
  "summary": zod.string().optional().describe('面向用户的一句话变更说明。'),
  "created_at": zod.string().datetime({"offset":true})
})),
  "undoable": zod.boolean().describe('是否仍可撤销。超过撤销窗口或目标已再次变化时为 false。'),
  "undone_at": zod.string().datetime({"offset":true}).nullish(),
  "created_at": zod.string().datetime({"offset":true})
})),
  "page": zod.object({
  "has_more": zod.boolean().describe('是否还有下一页。'),
  "next_cursor": zod.string().nullish().describe('不透明游标。App 不得解析其内容。')
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

/**
 * 目标在本批次之后再次变化时不可撤销，返回 VERSION_CONFLICT 并要求重新加载。
 * @summary 撤销一个变更批次
 */
export const UndoActivityBatchParams = zod.object({
  "batch_id": zod.string()
})

export const undoActivityBatchHeaderIdempotencyKeyMin = 8;
export const undoActivityBatchHeaderIdempotencyKeyMax = 128;



export const UndoActivityBatchHeader = zod.object({
  "Idempotency-Key": zod.string().min(undoActivityBatchHeaderIdempotencyKeyMin).max(undoActivityBatchHeaderIdempotencyKeyMax).describe('写请求幂等键，由客户端生成并在重试时保持不变。\n缺失时返回 IDEMPOTENCY_KEY_REQUIRED。\n')
})

export const UndoActivityBatchResponse = zod.object({
  "data": zod.object({
  "batch": zod.object({
  "id": zod.string(),
  "source": zod.enum(['user_form', 'capture_confirm', 'assistant_proposal', 'system']).describe('触发本批次的来源。'),
  "source_id": zod.string().nullish().describe('例如触发本批次的 Capture ID。'),
  "entries": zod.array(zod.object({
  "id": zod.string(),
  "action": zod.enum(['created', 'updated', 'deleted', 'restored', 'completed', 'reopened']),
  "resource_type": zod.string(),
  "resource_id": zod.string(),
  "title": zod.string().optional().describe('变更时的实体标题快照，用于列表展示。'),
  "summary": zod.string().optional().describe('面向用户的一句话变更说明。'),
  "created_at": zod.string().datetime({"offset":true})
})),
  "undoable": zod.boolean().describe('是否仍可撤销。超过撤销窗口或目标已再次变化时为 false。'),
  "undone_at": zod.string().datetime({"offset":true}).nullish(),
  "created_at": zod.string().datetime({"offset":true})
}),
  "affected_resources": zod.array(zod.object({
  "type": zod.enum(['task', 'event', 'project', 'note', 'record', 'tracker', 'task_list', 'capture', 'today', 'calendar', 'activity', 'assistant_thread', 'action_proposal', 'memory', 'memory_moment', 'recipe', 'meal_plan', 'diet_profile']).describe('受影响资源类型，App 据此精确失效缓存。'),
  "id": zod.string().nullable().describe('为空表示该类型的集合查询整体失效。')
}))
}),
  "meta": zod.object({
  "request_id": zod.string().describe('服务端为本次请求生成的追踪 ID，便于用户反馈与日志定位。')
}).describe('所有成功响应共有的元信息。')
})

