/**
 * Generated by orval 🍺
 * Do not edit manually.
 * 序事后台管理 API
 * 后台管理接口。**与公共 API 完全分开**：独立进程、独立端口、
 * 独立域名、独立 Cookie、独立数据库账号、独立契约。
 *
 * 分开不是为了整洁，是为了让「普通用户能不能碰到后台」这个问题
 * 有一个结构性的答案：碰不到，因为根本不在同一个进程上。
 *
 * ## 数据边界
 *
 * 后台**不返回任何用户正文**——笔记、会话消息、记忆、OCR 结果、
 * 转写文字、媒体内容一律不出现在任何响应里。能看到的是状态、
 * 数量、标识符和统计。
 *
 * 跨用户的列表与统计走 `admin` schema 里的脱敏读模型；
 * 单用户详情在该用户的 RLS 事务里执行，一次事务只绑一个用户。
 * Admin 的数据库账号是 NOBYPASSRLS 的，**它绕不过行级安全**，
 * 这条由集成测试守着。
 *
 * OpenAPI spec version: 0.1.0
 */
import * as zod from 'zod';


/**
 * @summary 使用概览
 */
export const AdminUsageOverviewQueryParams = zod.object({
  "from": zod.string().date().optional().describe('统计区间起始日（含），按后台报表时区切日。'),
  "to": zod.string().date().optional().describe('统计区间结束日（含）。')
})

export const AdminUsageOverviewResponse = zod.object({
  "data": zod.object({
  "range": zod.object({
  "from": zod.string().date(),
  "to": zod.string().date(),
  "timezone": zod.string().describe('报表时区。所有日期按它切日，界面上也按它显示。')
}),
  "dau": zod.number().int(),
  "wau": zod.number().int(),
  "mau": zod.number().int(),
  "feature_usage": zod.array(zod.object({
  "feature": zod.string(),
  "users": zod.number().int(),
  "events": zod.number().int()
}))
}),
  "freshness": zod.object({
  "data_as_of": zod.string().datetime({"offset":true}).nullable(),
  "aggregation_status": zod.enum(['fresh', 'stale', 'pending', 'failed'])
}).describe('读模型的新鲜度。后台的跨用户统计走聚合表而不是实时扫全库，\n因此\*\*必须把「这份数据算到什么时候」告诉使用者\*\*——\n不标出来的话，一个还没聚合的今天会被读成「今天没人用」。\n'),
  "meta": zod.object({
  "request_id": zod.string().describe('出错时报给用户的定位依据，日志里按它能查到整条调用链。')
})
})

/**
 * 分母为 0 时转化率返回空，界面显示“—”**而不是 0%**：
 * 没有样本和转化率为零是两回事。
 * @summary 漏斗
 */
export const AdminGetFunnelParams = zod.object({
  "funnel": zod.enum(['onboarding', 'capture', 'assistant'])
})

export const AdminGetFunnelQueryParams = zod.object({
  "from": zod.string().date().optional().describe('统计区间起始日（含），按后台报表时区切日。'),
  "to": zod.string().date().optional().describe('统计区间结束日（含）。')
})

export const AdminGetFunnelResponse = zod.object({
  "funnel": zod.string(),
  "data": zod.array(zod.object({
  "name": zod.string(),
  "users": zod.number().int(),
  "events": zod.number().int(),
  "conversion_from_previous": zod.number().nullish().describe('分母为 0 时为空，界面显示“—”而不是 0%。'),
  "conversion_from_start": zod.number().nullish()
})),
  "freshness": zod.object({
  "data_as_of": zod.string().datetime({"offset":true}).nullable(),
  "aggregation_status": zod.enum(['fresh', 'stale', 'pending', 'failed'])
}).describe('读模型的新鲜度。后台的跨用户统计走聚合表而不是实时扫全库，\n因此\*\*必须把「这份数据算到什么时候」告诉使用者\*\*——\n不标出来的话，一个还没聚合的今天会被读成「今天没人用」。\n'),
  "meta": zod.object({
  "request_id": zod.string().describe('出错时报给用户的定位依据，日志里按它能查到整条调用链。')
})
})
