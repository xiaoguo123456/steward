# 生产食谱步骤图补齐验收

日期：2026-09-07。用户授权：补齐生产步骤图片，移除“西红柿去皮小窍门”的“不在推荐位展示”标注。

## 原因与范围

生产 4267 道菜谱的 17317 个步骤没有图片地址；测试库对应 17297 张步骤图。两库剔除图片地址后的步骤内容与顺序指纹完全一致。生产旧数据版本为 `lanfan-2026-08`，测试为 `lanfan-2026-08.2`，代码发布未自动重跑内容导入。

补图来源为本地已授权归档中上传成功的对象键，按菜谱和步骤逐项匹配，不把测试库的其他字段覆盖到生产。无源图的 20 个步骤保留无图。生产和测试的 `rcp_lf8025` 仅移除指定标签，保留“烹饪基础”、其他原有标签和 `plan_excluded_reason=not_a_dish`。

这次是现有平台只读内容的数据修复，沿用 React Native、正式 Recipe 契约与既有图片 CDN，不改变 API、数据库结构、AI Prompt／模型／Schema、原生权限、H5 或第三方处理者。无需重新生成 Client、执行数据库结构迁移或打包 App。

## 执行与保护

先执行两环境预检，再执行测试环境，最后执行生产环境。备份不导出到本地，分别留在 `/opt/steward-test/backups/recipes-2026-09-07/before.csv` 与 `/opt/steward-prod/backups/recipes-2026-09-07/before.csv`；目录和文件通过 `umask 077` 创建，执行脚本也在同目录保留。备份字段为受影响行的 ID、步骤、标签和更新时间。

执行事务锁定相关行，校验菜谱数量、图片权利、步骤文字和顺序、已有图片与来源一致，备份成功后才更新；更新后确认其他全部菜谱字段未变化，失败自动回滚。未访问或修改用户的收藏、菜单、饮食档案等数据。

## 结果

| 环境 | 更新行数 | 新补图片 | 目标标注 |
|---|---:|---:|---|
| 测试 | 1 | 0 | 已移除 |
| 生产 | 4267 | 17297 | 已移除 |

再次执行预检，两环境均返回待更新 0 行、缺图 0 张、目标标注不存在，证明重复运行不会产生额外修改。20 个源资料没有步骤图的位置不在补图范围。

导入器同步过滤内部运营标记并更新为 `lanfan-2026-08.5`，原始标签保留在归档，分类与菜单筛选仍使用原规则。线上本次只做定向修复，未冒充完整 `.5` 版本重导。

验证：菜谱导入与分类的 63 项测试／自检全部通过，Python 编译检查、Git diff 空白检查通过，生产和测试数据库预检及事务完整性校验通过。分散抽查的 12 张步骤图片（含目标菜谱的两张步骤图）均返回 HTTP 200 与图片类型。Android 测试 App 的真实详情页已确认内部标注消失、两张步骤图正常显示；生产验证通过数据库读回和共用图片 CDN 访问完成，本轮未重新登录生产 App。服务器备份文件权限均为 0600，生产备份 4267 行、测试备份 1 行（均另含表头）。
